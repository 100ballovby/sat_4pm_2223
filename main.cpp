#include <iostream>
#include <vector>
#include <algorithm>  // если не работает find
#include <string>
using namespace std;


void print_vector(vector<int> &v) {
    for (int i = 0; i < v.size(); i++) {
        cout << v[i] << ' ';
    }
    cout << endl;
}

int main() {

    return 0;
}